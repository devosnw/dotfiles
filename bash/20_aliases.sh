#
# aliases
#

if __is_os Linux; then
  # tell `ls` to use colors. Also see `$CLICOLOR`
  alias ls='ls --color=auto'
fi
alias l='ls -l'
alias ll='ls -l'
alias lal='ls -al'
alias ..='cd ../'
alias ...='cd ../../'
alias ....='cd ../../../'

