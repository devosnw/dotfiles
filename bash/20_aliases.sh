#
# aliases
#

if __is_os Linux; then
  # tell `ls` to use colors. Also see `$CLICOLOR`
  alias ls='ls --color=auto'
fi
alias l='ls -lh'
alias ll='ls -lh'
alias lal='ls -alh'
alias ..='cd ../'
alias ...='cd ../../'
alias ....='cd ../../../'

