# If set to ‘on’, Readline displays possible completions using different
# colors to indicate their file type. The color definitions are taken from the
# value of the LS_COLORS environment variable. The default is ‘off’.
set colored-stats on

# If set to `on`, Readline performs filename matching and completion in a
# case-insensitive fashion. The default value is ‘off’.
set completion-ignore-case on

# If set to ‘on’, tilde expansion is performed when Readline attempts word
# completion. The default is ‘off’.
set expand-tilde off

# If set to `on`, completed directory names have a slash appended. The default
# is `on`.
set mark-directories on

# This alters the default behavior of the completion functions. If set to
# `on`, words which have more than one possible completion cause the matches
# to be listed immediately instead of ringing the bell. The default value is
# `off`.
set show-all-if-ambiguous on

# If set to `on`, words which have more than one possible completion without
# any possible partial completion (the possible completions don't share a
# common prefix) cause the matches to be listed immediately instead of ringing
# the bell. The default value is `off`.
set show-all-if-unmodified on

# If set to ‘on’, a character denoting a file’s type is appended to the
# filename when listing possible completions. The default is ‘off’.
set visible-stats on

# Incremental searching with Up and Down. Type 'ssh <up>' to get the last
# 'ssh' command you used
"\e[A": history-search-backward
"\e[B": history-search-forward

